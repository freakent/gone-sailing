{% assign format = include.format | default: "images" %}
{% assign baseurl = include.baseurl | default: site.content_baseurl | append: "/:format" %}

{% assign first_char = include.src | slice:0 %}
{% if first_char == "/" %}
    {% assign src = baseurl | append: include.src | replace: ":format", format  %}
{% else %}
    {% assign src = baseurl | append: "/" | append: include.src | replace: ":format", format  %}
{% endif %}
<img class="{{ include.class }}" src="{{ src }}" alt="{{ include.alt }}" style="max-height: 500px; object-fit: cover;">